# AMIP Python dependencies
# Tested on: Python 3.12 (target), also works on 3.11, 3.13, 3.14
# Platform: x86_64 and aarch64 (ARM) Linux — all packages have pre-built wheels

duckdb>=1.1.0
fastapi>=0.110.0
uvicorn>=0.29.0
requests>=2.31.0
pyproj>=3.6.0
openpyxl>=3.1.0

# Required by scripts/ingest_wholesale_prices.py to scrape AIP terminal gate
# prices. Present on the VPS venv (4.14.3) but never listed here, so a clean
# venv silently lost it and the wholesale job failed with exit 1.
beautifulsoup4>=4.12.0
